apiVersion: v1
kind: Service

metadata:
  name: quicktick-service

spec:

  selector:
    app: quicktick

  type: NodePort

  ports:

  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080
